{
  "case_id": "c07-mcp-unsafe-url-scheme",
  "group": "C",
  "guards": ["mcp"],
  "expected_findings": [
    {"guard": "mcp", "rule": "mcp_config_risky_pattern", "path": ".mcp.json", "reason": "unsafe_url_scheme"}
  ],
  "forbidden_findings": [],
  "notes": "MCP remote endpoint uses an unsafe URL scheme."
}
